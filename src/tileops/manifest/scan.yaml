# scan.yaml -- manifest entries for the 'scan' family.
#
# Source of truth for op interfaces in this family. Loaded and merged with
# other family files by tileops.manifest at runtime. See docs/design/manifest.md
# for the full schema.

CumsumFwdOp:
  ref_api: "torch.cumsum"
  family: scan
  status: implemented
  torch_compile_fullgraph: true

  signature:
    inputs:
      x: {dtype: "float16 | bfloat16 | float32"}
    outputs:
      y: {dtype: "same_as(x)"}
    params:
      dim: {type: int, default: -1}
    shape_rules:
    - "-x.ndim <= dim < x.ndim"
    - "y.shape == x.shape"

  workloads:
    # Hidden state cumulative sum (Llama-3.1-8B)
  - {x_shape: [2048, 4096], dtypes: [float16, bfloat16], label: "hidden-state-scan"}
    # Long sequence cumulative sum
  - {x_shape: [64, 32768], dtypes: [bfloat16], label: "long-seq-scan"}

  roofline:
    vars:
      M: "product(x.shape[:dim % x.ndim]) * product(x.shape[dim % x.ndim + 1:])"
      N: "x.shape[dim % x.ndim]"
    # N-1 additions per scan line
    flops: "M * N"
    # Read x + write y
    bytes: "2 * M * N * elem_bytes"

  source:
    kernel: backends/ascend/src/tileops_ascend/families/scan.py
    op: tileops/ops/reduction/cumulative.py
    test: tests/ops/test_cumulative.py
    bench: benchmarks/ops/bench_cumulative.py
    bench_manifest_driven: true

CumprodFwdOp:
  ref_api: "torch.cumprod"
  family: scan
  status: implemented
  torch_compile_fullgraph: true

  signature:
    inputs:
      x: {dtype: "float16 | bfloat16 | float32"}
    outputs:
      y: {dtype: "same_as(x)"}
    params:
      dim: {type: int, default: -1}
    shape_rules:
    - "-x.ndim <= dim < x.ndim"
    - "y.shape == x.shape"

  workloads:
    # Hidden state cumulative product (Llama-3.1-8B)
  - {x_shape: [2048, 4096], dtypes: [float16, bfloat16], label: "hidden-state-scan"}
    # Long sequence cumulative product
  - {x_shape: [64, 32768], dtypes: [bfloat16], label: "long-seq-scan"}

  roofline:
    vars:
      M: "product(x.shape[:dim % x.ndim]) * product(x.shape[dim % x.ndim + 1:])"
      N: "x.shape[dim % x.ndim]"
    # N-1 multiplications per scan line
    flops: "M * N"
    # Read x + write y
    bytes: "2 * M * N * elem_bytes"

  source:
    kernel: backends/ascend/src/tileops_ascend/families/scan.py
    op: tileops/ops/reduction/cumulative.py
    test: tests/ops/test_cumulative.py
    bench: benchmarks/ops/bench_cumulative.py
    bench_manifest_driven: true
